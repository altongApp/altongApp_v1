<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 버튼 배경 (초록색) -->
    <item
        android:top="0dp"
        android:left="0dp"
        android:right="2dp"
        android:bottom="2dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/primary_green_dark" />
            <corners android:radius="8dp" />
        </shape>
    </item>

</layer-list>